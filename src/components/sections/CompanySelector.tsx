"use client";

export function CompanySelector() {
  // This component has been disabled to remove the initial splash/selector page on load.
  // The company boxes are accessible via the CompanyGrid component on the main page.
  return null;
}
